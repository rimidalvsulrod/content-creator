{
  "name": "prompter",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-ffmpeg.mjs && vite --host",
    "build": "node scripts/copy-ffmpeg.mjs && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
